import Requests from "~/middleware/Requests";
const BASE_URI = "https://free-to-play-games-database.p.rapidapi.com/api/game";
const REQUEST_EXECUTOR = new Requests(BASE_URI)

export default class API{
 constructor() {

 }
 getStudents()
}
